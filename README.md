# newfile

$ pkg update && pkg upgrade -y

$ pkg install python -y

$ pip install requests

$ pip install termcolor

$ pkg install python3

$ git clone https://github.com/ibrahimadaballi/newfile.git

$cd newfile

$ pip install -r requirements.txt

$ python newfile.py
